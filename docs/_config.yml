# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: brain
tagline: isn't it comfortable?
email: admin@inversity.dev
description: >- # this means to ignore newlines until "baseurl:"
  Documentation of the brain.
baseurl: "" # the subpath of your site, e.g. /blog
domain: inversity.dev
url: "https://inversity.dev" # the base hostname & protocol for your site, e.g. http://example.com
# twitter_username: jekyllrb
# github_username:  jekyll
show_downloads: false
show_github_link: false

# Sitewide default OG/preview image (overridable per-page via `image:` frontmatter)
image: /favicon/preview.jpg

# Apply noindex to every page by default. Override on a per-page basis with
# `noindex: false` in frontmatter if you ever want a page to be indexable.
defaults:
  - scope:
      path: ""
    values:
      noindex: true
      sitemap: false

# Build settings
remote_theme: pages-themes/midnight@v0.2.0

# add this line to the plugins list if you already have one
plugins:
- jekyll-feed
- jekyll-remote-theme
- jekyll-seo-tag

# Collections
collections:
  stuff:
    output: true
    permalink: /stuff/:name/

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
